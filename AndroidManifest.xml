<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.privacyawareinterface">

    <!-- 🔒 Required Permissions -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PrivacyAwareInterface"
        tools:targetApi="31">

        <!-- 🏠 Launcher (Role Selection First) -->
        <activity
            android:name=".RoleSelectionActivity"
            android:exported="true"
            android:label="Role Selection"
            android:theme="@style/Theme.PrivacyAwareInterface">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MainActivity" android:exported="false" />
        <activity android:name=".ParentDashboardActivity" android:exported="false" />
        <activity android:name=".ChildDashboardActivity" android:exported="false" />
        <activity android:name=".ChildLoginActivity" android:exported="false" />
        <activity android:name=".GameLearningActivity" android:exported="false" />
        <activity android:name=".TheoryLearningActivity" android:exported="false" />
        <activity android:name=".VideoLearningActivity" android:exported="false" />
        <activity android:name=".VideoPlayerActivity" android:exported="false" />
        <activity android:name=".WarningActivity" android:exported="false" />

        <!-- ========================================== -->
        <!-- 🚨 ALWAYS-ON PROTECTION SERVICES -->
        <!-- ========================================== -->

        <!-- 🔍 Accessibility Service -->
        <service
            android:name=".monitor.AppBlockerAccessibilityService"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- ⚙️ Foreground Monitoring Service -->
        <service
            android:name=".monitor.AppBlockerService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- 🔁 Boot Restart Receiver -->
        <receiver
            android:name=".monitor.BootReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
